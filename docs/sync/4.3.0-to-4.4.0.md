# Upstream diff: h3-4.3.0 -> h3-4.4.0

Symbols scanned: 421 old, 425 new. Changed/added/removed: 8.

## Library and header symbol changes

| Status | Kind | C symbol | Go mapping | Review |
|---|---|---|---|---|
| changed | func | `h3Index.c::_hasDeletedSubsequence` | h3index__hasDeletedSubsequence.go | port the diff to the mapped file(s) |
| changed | func | `h3Index.c::compactCells` | h3index__compactCells.go | port the diff to the mapped file(s) |
| added | func | `h3Index.c::constructCell` | h3index_constructCell.go | port the diff to the mapped file(s) |
| changed | func | `h3Index.c::describeH3Error` | h3index__describeH3Error.go | port the diff to the mapped file(s) |
| added | func | `h3Index.c::getIndexDigit` | h3index_getIndexDigit.go | port the diff to the mapped file(s) |
| added | table | `h3Index.c::isBaseCellPentagonArr` | — | NEW: port + attribute + test |
| added | func | `h3Index.c::isValidIndex` | h3index_isValidIndex.go | port the diff to the mapped file(s) |
| changed | func | `vertex.c::vertexRotations` | vertex__vertexRotations.go | port the diff to the mapped file(s) |

## Upstream test-file changes (src/apps/testapps)

| Status | File | Ported to Go? | Review |
|---|---|---|---|
| changed | `testBBoxInternal.c` | yes | review the test diff; port new/changed cases |
| changed | `testCellToLocalIj.c` | yes | review the test diff; port new/changed cases |
| added | `testConstructCell.c` | yes | review the test diff; port new/changed cases |
| changed | `testDescribeH3Error.c` | yes | review the test diff; port new/changed cases |
| changed | `testH3Index.c` | yes | review the test diff; port new/changed cases |
| added | `testIndexDigits.c` | yes | review the test diff; port new/changed cases |
| changed | `testVertexInternal.c` | yes | review the test diff; port new/changed cases |

Unmapped changed symbols: 0

---

## Review resolution (audit completed 2026-07-11)

Generated by `make upstream-diff FROM=4.3.0 TO=4.4.0`; every row above was
reviewed against the Go port. Disposition:

### Library symbols

| C symbol | Disposition |
|---|---|
| `h3Index.c::getIndexDigit` (added) | Ported in the initial 4.4.0 sync (`h3index_getIndexDigit.go`, public `Cell.IndexDigit`). |
| `h3Index.c::constructCell` (added) | Ported in the initial sync (`h3index_constructCell.go`, public `ConstructCell`). |
| `h3Index.c::isValidIndex` (added) | Ported in the initial sync (`h3index_isValidIndex.go`, public `IsValidIndex`). |
| `h3Index.c::isBaseCellPentagonArr` (added, file scope) | Table already existed at package scope in the Go port (`h3index_constants.go`); the 4.4.0 change merely hoisted it out of `_hasDeletedSubsequence`. Attribution comment added so tooling maps it. **No behavior change.** |
| `h3Index.c::_hasDeletedSubsequence` (changed) | Change = removal of the function-local static table (hoisted above). Go body already matched the 4.4.0 shape. **No behavior change.** |
| `h3Index.c::describeH3Error` (changed) | Ported in the initial sync (bounds check `err < H3_ERROR_END`, 4 new messages). |
| `h3Index.c::compactCells` (changed) | Change = wrapping the defensive `loopCount` guard in `DEFENSEONLY(...)` (compiled out unless `H3_DEFENSE`). The Go port keeps the guard unconditionally, equivalent to a defense-enabled C build. **No release-behavior change; intentionally not mirrored** (Go keeps the cheap guard). |
| `vertex.c::vertexRotations` (changed) | Change = `for` → `while` + `ALWAYS`/`NEVER` coverage macros; release expansion of those macros is the identity, so semantics are unchanged. Go keeps the loop form. **No behavior change.** |

Public header (`h3api.h.in`): 4 new error codes (16–19) + `H3_ERROR_END`
sentinel + 3 new declarations — all ported (sentinels `ErrIndexInvalid`,
`ErrBaseCellDomain`, `ErrDigitDomain`, `ErrDeletedDigit`).

### Upstream tests — the gap the initial sync missed

The initial Phase-7 sync ported the implementation delta but did **not**
review the upstream test delta. Closed by this audit:

| Upstream test change | Go port |
|---|---|
| `testConstructCell.c` (new) | `testConstructCell_test.go` — full table incl. the res-15 vector `0x8f754e64992d6d8` and pentagon deleted-subsequence cases; round-trip over all cells at res 0–4. |
| `testIndexDigits.c` (new) | `testIndexDigits_test.go` — digit extraction across all resolutions and set digits; domain errors. |
| `testH3Index.c` (changed) | new `isValidIndex` suite → covered by `TestIsValidIndex` (cell/edge/vertex/zero/all-ones), high-bit corruption case added. |
| `testDescribeH3Error.c` (changed) | `H3_ERROR_END`, `H3_ERROR_END+1`, and codes-are-not-indexes tests added to `h3index__describeH3Error_test.go`. |
| `testCellToLocalIj.c` (changed) | overflow tests extended to loop over every resolution (was res 2 only) in `testCellToLocalIj_test.go`. |
| `testBBoxInternal.c` (changed) | `bboxHexEstimate_invalidRes` added to `bbox_testBBoxInternal_test.go`. |
| `testVertexInternal.c` (changed) | `directionForNeighbor_invalid` added to `vertex__vertexInternal_test.go`. |

The later comprehensive v4.4.0 test audit supersedes the original limited
test-file review above. All 23 upstream libFuzzer/AFL harness domains now map
to native `FuzzUpstream*` targets, and the filter/CLI scope was recorded at
the time as not applicable to the then-library-only module. See
`docs/ported-c-tests.md` and `docs/upstream-test-inventory.csv` for the
case-level result.

*(Update, 2026-07: the repository has since gained the upstream-compatible
`h3` CLI, and the 170 upstream CLI cases are now dispositioned `full`
against it — see `docs/cli-compatibility.md`.)*
