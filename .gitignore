# Binaries for programs and plugins
*.exe
*.exe~
*.dll
*.so
*.dylib

# Locally built h3 CLI (make build-cli)
/h3

# Test binary, build with `go test -c`
*.test

# Output of the go coverage tool, specifically when used with LiteIDE
*.out

.idea/

coverage*.html
coverage*.out

.claude/

.gocache/